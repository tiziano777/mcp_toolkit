name: filesystem-mcp
version: 1.0.0
schema: v1

mcpServers:
  - name: Filesystem # Puoi usare questo nome quando parli con Continue
    command: npx
    args:
      - "-y"
      - "@modelcontextprotocol/server-filesystem"
      #- "C:\\Users\\tifinizzi\\Desktop\\mcp_toolkit\\workspace" # <-- Questo è il tuo path di workspace per windows
      - "/home/tiziano/progetti/mcp_toolkit/workspace" # <-- Questo è il tuo path di workspace per linux
      # Puoi aggiungere altre directory consentite qui, se necessario
      # Esempio: - "/path/to/another/allowed/directory"