name: Docker MCP Git
version: 0.0.1
schema: v1

mcpServers:
  - name: Docker MCP Git # Questo è il nome che userai per interagire con Continue
    command: docker
    args:
      - run
      - --rm
      - -i
      - --mount
      - type=bind,src=/home/tiziano/progetti/mcp_toolkit,dst=/git_repo # <-- PERCORSO FISSO qui
      - mcp/git