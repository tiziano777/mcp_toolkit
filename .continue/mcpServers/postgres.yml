name:  Postgres MCP
version: 1.0.0
schema: v1
mcpServers:
    name: Postgres
    command: "npx"
    args: 
      - "-y"
      - "@modelcontextprotocol/server-postgres"
      # replace with your actual connection string postgresql://user:password@host:port/db-name
      - "postgresql://postgres:postgres@localhost:5432/DB_NAME" # <-- This is your connection string