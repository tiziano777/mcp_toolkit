name:  postgres-mcp
version: 1.0.0
schema: v1

mcpServers:
  name: Postgres
  command: docker
  args: 
    - "run"
    - "-i"
    - "--rm"
    - "mcp/postgres"
    - "postgresql://postgres:postgres@localhost:5432/DB_NAME"
